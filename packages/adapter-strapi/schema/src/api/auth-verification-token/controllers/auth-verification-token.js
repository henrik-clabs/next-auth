'use strict';

/**
 * auth-verification-token controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::auth-verification-token.auth-verification-token');
