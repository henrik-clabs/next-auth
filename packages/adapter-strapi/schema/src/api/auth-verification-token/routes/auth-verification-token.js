'use strict';

/**
 * auth-verification-token router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::auth-verification-token.auth-verification-token');
